# Project Rules

- Do not create, delete, edit, or upload GitHub Releases unless the user explicitly asks for a GitHub release or asks to apply/publish an update.
- Do not bump the app version solely to make the update checker show a new update unless the user explicitly asks to release or publish an update.
- Building installers, release archives, or local test packages is allowed only as local verification unless the user explicitly requests publishing them.
